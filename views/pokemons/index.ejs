<% // work more:  %>  
<% // to do when the pokemon is removed from favorite <pokemon> removed from favorite will popup %>


<% if(added) { %>
    <h5><%= added %>  added to Favorites</h5>
<% } %>  

<h4>Your Favorite pokemons:</h4>
</br></br>
<ul>
    <% myPokes.forEach(poke => { %>
        <div class="well2">
            </br>
            <a href="/pokemon/<%= poke.name %>"><img src="<%= poke.imgUrl %> " alt="Pokemon Image"></a>
            </br></br>
            <li><a class="indexPokeName" href="/pokemon/<%= poke.name.toLowerCase() %>"> <%= poke.name.charAt(0).toUpperCase() + poke.name.slice(1) %></a></li>
            </br>
        </div>
    </br>
    <% }) %> 
</ul>